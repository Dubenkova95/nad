импорт ява.яз.Математика;
импорт ява.утиль.Сканер;

класс чипсы
{
    публичный статический пустота главный(строка[] аргументы)
    {
        Сканер в = новый сканер(система.в);
        инт фишки = в.nextInt();
 
        если(чипы % 4 == 0 || фишки == 1)
        {
            Системы.из.код println("да");
        }
        еще
        {
            Системы.из.код println("нет");
        }
    }
}
